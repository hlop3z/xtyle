/**
 * Router for handling routing and rendering based on the provided props.
 *
 * @param {Props} props - The props object containing the following attributes:
 *
 * @param {Function} props.callback - The callback function to be called when routing is triggered.
 * @param {boolean} [props.history] - Optional boolean flag to enable history API for routing.
 * @param {string} [props.baseURL] - Optional base URL for the router.
 * @param {any} [props.routes] - Optional routes object containing route definitions.
 *
 * @returns {void} - This function does not return any value, as it handles routing internally.
 */
