/**
 * Represents the window's dimensions and device size classification.
 *
 * @property {number} x - The current width of the window.
 * @property {number} y - The current height of the window.
 * @property {string} size - The current size classification of the window ('xs', 'sm', 'md', 'lg', 'xl').
 * @property {boolean} mobile - Indicates if the device is considered mobile based on the width.
 * @property {Function} is - A function to check if the current size matches the specified classification.
 *                            Takes a size classification ('xs', 'sm', 'md', 'lg', 'xl') as an argument and returns a boolean.
 *
 */
